import { userRepository } from "../schema/user.schema.js";

// auth
export const auth = async (req, res) => {
  // fetch the user with specific google id
  // if the data comes with specific google id {
  // login
  // } else {
  // create
  // }
};
